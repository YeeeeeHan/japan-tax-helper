# ===============================================
# GEMINI API TIER CONFIGURATION
# ===============================================
# FREE TIER:  5 RPM, 250K tokens/min, 1000 req/day
# PAID TIER: 150 RPM, 4M tokens/min, 10K req/day (30x increase!)
#
# HOW TO UPGRADE TO PAID TIER:
# 1. Visit https://aistudio.google.com/apikey
# 2. Click "Enable billing" on your API key
# 3. Link a Google Cloud billing account ($20/month minimum)
# 4. Same API key gets higher limits automatically - no need to regenerate
# 5. Update RATE_LIMIT_MAX=100 below
#
# See .claude/PAID_TIER_UPGRADE.md for detailed instructions
#
# Google Gemini API Key (server-side only - NEVER prefix with NEXT_PUBLIC_)
GEMINI_API_KEY=your_gemini_api_key_here

# Rate limiting (requests per minute per IP)
# Default: 100 (paid tier) | Change to 5 for free tier
RATE_LIMIT_MAX=100

# Gemini API Tier (affects parallel processing concurrency)
# paid: 5 concurrent uploads | free: 2 concurrent uploads
# This is NEXT_PUBLIC_ because it configures client-side concurrency behavior
# Default: paid (change to 'free' if using free tier)
NEXT_PUBLIC_GEMINI_TIER=paid

# Application URL (safe to expose to client)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Anthropic Claude API Key
ANTHROPIC_API_KEY=

# Claude model selection (optional, defaults to claude-sonnet-4-20250514)
# Options: claude-sonnet-4-20250514 (default), claude-opus-4-20250514 (best), claude-3-5-sonnet-latest
# CLAUDE_MODEL=claude-sonnet-4-20250514

# Qwen VL via hosted providers (choose one)
DEEPINFRA_API_KEY=

# OCR Strategy for production (optional, defaults to gemini-2.5-flash)
# Options:
#   gemini-2.5-flash (RECOMMENDED - stable, 84% cheaper than 2.0, no deprecation risk)
#   gemini-2.5-flash-lite (BUDGET - 95% cheaper, good for clear receipts)
#   gemini-2.0-flash (DEPRECATED - retires March 3, 2026)
#   gemini-1.5-flash (older generation, still supported)
#   claude-sonnet (best for Japanese, requires ANTHROPIC_API_KEY)
#   qwen-vl (cheapest, requires DEEPINFRA_API_KEY)
# Set this in Vercel Dashboard to change which AI model processes receipts
# OCR_STRATEGY=gemini-2.5-flash